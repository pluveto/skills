# Rust 开发规范

## 1. 设计与架构

- 必须遵循 SOLID，并保持干净代码。
- 所有 `pub` 类型必须标注 `#[non_exhaustive]`，除非可以确定永远不会增加变体或字段。
- 谨慎使用游离函数。只允许放在私有 `mod helper` 中；如果只使用一次且逻辑短小，优先局部闭包。
- 禁止文件膨胀。单个源文件的纯代码部分不得超过 700 行，不含注释和空行。

## 2. 类型系统与建模

- 优先使用 `From` / `TryFrom` 做转换，除非这会违反 SOLID；受孤儿规则限制时可使用命名转换函数。
- 所有 struct / trait 的设计都必须有明确理由，禁止滥用 `Clone` / `Copy`。
- 能使用 `strum` 时尽量使用，避免手动映射。
- 业务类型实现 `Eq` 和 `Hash` 时，必须显式说明等价性规则。
- 禁止对原生或其他类型做无业务语义的简单封装。
- 禁止使用 `as` 进行数值转换，必须使用 `From` / `TryFrom`。
- 禁止 `pub use *`；所有导出项和 `pub` 修饰都必须判断必要性。
- 除非确实是 `bool` 语义，否则不要用 `bool` 表达状态。
- 时间类型统一使用 `time::OffsetDateTime`；新代码禁止引入 `chrono`。公共模型时间字段按 Unix 毫秒序列化时，使用 `time::serde::timestamp::milliseconds`。
- 外部可构造且字段较多的 `pub struct` 必须提供 builder，禁止长期保留多参数 `new(...)`。默认使用 `derive_builder`，并将 `too_many_arguments` 阈值固定为 5。
- 一般情况下，游离函数只能出现在 `internal.rs` 或 `helper.rs` 等私有工具区文件中，且必须是纯工具函数，不得承载业务语义。特殊情况下允许全局函数，但必须在文件头部写明 `RATIONALE`。不要通过 `impl` 结构体伪装全局函数。
- 能使用 `Into<&'static str>` 的，不要写 `fn as_str`。
- 类型别名仅用于简化复杂泛型签名，不用于业务语义。
- 所有 Trait 的使用都必须经过论证，不得因为方便就随手使用 `Clone` 等。`Copy` 只用于平凡 POD 类型和确有必要的性能优化。
- 避免静态生命周期依赖，`&'static str` 只用于常量和配置，不用于数据传递。

## 3. 错误处理与健壮性

- 使用 `thiserror` 派生业务错误；`anyhow` 仅用于顶层二进制，库代码禁止暴露 `anyhow`.
- 禁止在 `map_err` 里丢弃错误信息，每层转换只增加信息，不减少信息。
- 库代码禁止用 `String` 或 `&str` 直接表示错误，错误必须结构化为枚举或错误类型，`Display` 只负责最外层文本化。
- 超时必须是独立错误变体，例如区分 `Error::Timeout` 和 `Error::Network`，便于上层判断是否重试。
- `unwrap()` 只允许在测试和不可恢复初始化里使用。生产代码里的每个 `unwrap` 都必须有注释解释为什么不会失败。
- `expect()` 的消息必须包含失败后无法恢复的原因，而不是只描述“应该是什么”。
- 禁止低质量测试，例如冗余的 `set + get + assert` 断言。

## 4. 异步与并发

- 禁止 `block_on`，异步上下文里不能同步等待另一个异步任务。
- 所有 `select!` 分支必须公平。
- 取消必须安全：所有 `async fn` 都必须在 `.await` 点持有可丢弃状态，不能假设 future 一定会执行完成。
- `CancellationToken` 必须传递到所有子任务；关闭时要优雅取消所有 WS 读取、定时器和重连循环。

## 5. 性能、序列化与 IO

- 在 hot path 里禁止 clone 有堆分配的类型。
- 所有外部边界序列化都使用 `serde`；非边界代码禁止依赖 `serde`。如果类型是公共的，尽量把序列化做成可选 feature。
- `Display` 用于用户，`Debug` 用于开发者。

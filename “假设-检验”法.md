# 修 Bug 行为约束

## 核心原则

修 Bug 必须采用“假设-检验”法，禁止“猜测-尝试”法。

## 工作方式

- 基于现有证据提出一个或少数几个可证伪的故障假设。
- 对每个假设都要明确：假设、验证手段、预期结果、实际结果、结论。
- 每次只验证有限变量，优先通过观测、对照、日志、最小复现来缩小范围。
- 不得凭直觉穷举修复方案，不得把“试试看”作为主要策略。
- 如果信息不足，先补充观测与定位，再继续修改。

## 修改约束

- 不得无约束地同时修改多个可能原因。
- 任何改动都必须可回退。
- 不得破坏恢复到初始状态的能力，即使初始状态本身存在 Bug。

## Git 约束

- 必须使用 Git 管理整个修复过程。
- 每当问题范围成功缩小一次，就必须提交一次 commit。
- 每个 commit 都应作为一个可回退的检查点。

## 目标

目标不是线性穷举，而是用最少实验快速缩小问题空间。
